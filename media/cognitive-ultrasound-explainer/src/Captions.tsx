// PPT v2 intentionally has no burned-in narration. Presenter cues: docs/presenter-notes.md.
// Kept as a harmless compatibility entry; the composition does not import it.
export const Captions = () => null;
