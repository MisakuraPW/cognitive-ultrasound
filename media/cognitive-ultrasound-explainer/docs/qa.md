# PPT v2 检查记录

2026-09-22；版本：55 秒 PPT 辅助动画。上一版检查记录保留在 qa-v1.md。

## 资料与修改边界

沿用已核对的 CASL 论文与架构审计；再次确认当前帧重建后选择下一帧扫描位置。复用原探头、几何、固定种子纹理，未调用训练或科研推理。所有改动限制在本动画项目。

## 画面与时序

- 已检查 22 个初版预览时刻，以及最终 MP4 的 76 张关键和连续过程帧。
- 实际在 Studio 逐帧检查候选方向、42 秒换帧、48.2 秒换帧、六条新测量及后续估计。Studio 的播放开关本次未推动时间，所以没有将其当作连续播放通过；改用完整视频解码、连续帧检查和运动差异记录核对动态过程。
- scan-sequence.png、transition-first.png、transition-second.png 展示发射与多个深度回波、逐线累积、两次选线/换帧/重建的连续过程，不仅核对单张关键帧。
- 修正实测标签：引线跟随每一帧确实采到的方向。修正估计标签：新帧尚无估计时隐藏，随估计图一起出现。
- 纯白背景；没有独立标题、完整字幕、页码、页脚、右侧解释正文或总结页。超声黑底裁切在扇形内部。
- 已人工查看 960×540 的取舍、不确定性检查图，文字、实线/虚线、橙色热区清楚，没有遮挡或字体缺失。主要标签原始字号 35–44 px。
- 范围与深度对照在同一画面同时展示，不进行多次拆页讲解；0–24 秒覆盖全部前半段。
- 稀疏采集到估计保留约 13 秒；不确定性部分 18 秒，两次换帧均为六条实测线；候选方向不计作当前帧观测。
- 最后 53–55 秒保持同一重建图、已测线和剩余热区；没有额外结尾页。

## 最终编码验证

- H.264，1920×1080，30 fps，55.000 秒，1650 帧，无音轨。实际像素格式 yuvj420p：8-bit 4:2:0 全范围。
- FFmpeg 全片解码通过；OpenCV 解码并分析全部 1650 帧的相邻运动差异，记录在 frame-motion.csv。
- 17.9 / 18.1 秒，较长采集周期一侧平均 RGB 差 0.00793，较短周期一侧 4.37785。说明前者持帧、后者较早更新；两个来源使用相同运动时钟。
- 53.0 / 54.9667 秒全图平均 RGB 差 0.30055 / 255；末尾两秒相邻帧平均灰度差最大 0.14073 / 255，符合静止源画面的有损编码微差。
- 关键帧白色角落最低通道值 255；最终联系表和缩小图均从成片抽取。
- 旧 MP4 SHA-256 保持 C47165A8B2CB4CD97B5036AFC7E2B9ECE5A52293CC378C902FF0F4E3D3F53BA6。
- ESLint + TypeScript、git diff --check 通过。MP4 和渲染缓存位于被忽略的 outputs/ppt-v2 路径下。

验证入口 scripts/verify-video.py；机器可读结果在 ppt-v2/qa-results.json 和 ffprobe.json。人工检查只能核对本机缩小预览，不替代实际答辩投影环境的亮度与对比度检查。
