# 准备性流水线本地检查（2026-09-19）

检查环境：Windows、本项目 `.venv`，TensorFlow/JAX 分开进程，CPU 合成数据。
未连接 AutoDL、未消耗云 GPU、未在这台机器上重新转换 EchoNet。

已覆盖：

- 原版 warm solver 与全指导新实现的数值对照。
- JIT 和 vmap 下稀疏指导实际执行的梯度调用次数；不是仅把 omega 设成零。
- 保存/恢复随机 key、测量历史、后验粒子和动作；分支历史不共享可变容器。
- 模拟第三帧中断，前两帧原文件保持不变，恢复从第三帧继续。
- 任务超时终止、累计时间预算、已完成任务不重复、依赖不满足时跳过、重复运行锁。
- 风险探针训练病例隔离；修改确认集标签不会改变训练所得系数、归一化和预算阈值。
- 缺少完整原版或固定历史检查不通过时，加速候选不得晋级。
- BF 训练时冻结组件、梯度有限、优化器断点恢复与连续训练一致；准备性封装的 codec/prior/filter 三阶段与资格检查全部走通。
- 自动 CSV/Markdown/图像生成、结果打包读回；定性图布局人工查看。
- Ruff、Python 编译、Bash 语法、Git diff 空白检查。

这些是工程功能检查，不是医学图像效果、GPU 速度、12 小时内完成所有任务的证明。
真实 TBIG 环境和 AutoDL GPU 版本的本批次运行尚未验证；服务器入口会先做 GPU 预检，单项失败有日志和限时保护。
默认配置只运行小规模准备性任务，结果达不到门槛时保留阴性结论与失败原因，不自动扩张训练预算。
