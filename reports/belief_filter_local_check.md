# 信念滤波初版本地检查（2026-09-14）

这是 `thesis_inspired_bf_v1` 的功能和速度检查，不是学长论文指标复现。
本地 Windows CPU / TensorFlow 2.20.0，未使用云端 4090。

- 独立功能测试：4 项通过，约 30 秒。包括无未观测真值泄漏、每帧观测清空、
  冻结权重和梯度、完整三阶段小步训练、恢复优化器与连续训练一致、评估图像输出。
- 真实数据检查：沿用本地已转换的 1 个 train 病例、1 个 val 病例；不读 test。
  使用默认网络尺寸和每步连续 3 帧，各阶段 5 步，再评估 val 的前 3 帧，全部完成。
  这些样本和步数不能用于估计收敛效果。

| 阶段 | 本阶段可训练参数 | 单步时间中位数（去掉首步，剩余 4 步） |
| --- | ---: | ---: |
| codec | 42,777 | 0.328 秒 |
| prior | 4,952 | 0.385 秒 |
| filter | 172,673 | 3.081 秒 |

以上时间含一个片段读取和训练更新，不含验证、初始化和 checkpoint 保存。
按相同速度外推默认 100/100/200 步，纯训练约 11.5 分钟；
按三阶段各 10,000 步作算术外推，分别约 0.91/1.07/8.56 小时。
后者不是推荐训练预算，也不是达到可用质量的时间；
扩大网络、片段长度、批量、病例范围后必须重新计时。
GPU 速度未测量，不承诺固定加速倍数。

本地原始日志及计时：`tmp/belief_real_cpu_v1/`，合成测试产物：
`tmp/belief_filter_tests_v2/`。这些临时文件和权重不纳入 Git。

三阶段权重和验证图只是程序行为验证产物，不能用作正式基线权重。
